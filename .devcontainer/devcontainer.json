{
  "name": "RVK Backend",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "postCreateCommand": "cd backend && npm install",

  "postStartCommand": "sudo service postgresql start && sudo -u postgres psql -c \"ALTER USER postgres WITH PASSWORD 'postgres';\" && sudo -u postgres psql -tc \"SELECT 1 FROM pg_database WHERE datname='myapp'\" | grep -q 1 || sudo -u postgres createdb myapp && cd backend && npx prisma db push",

  "forwardPorts": [3000, 5432, 5555],

  "portsAttributes": {
    "3000": {
      "label": "NestJS API",
      "onAutoForward": "notify",
      "visibility": "public"
    }
  },

  "remoteEnv": {
    "DATABASE_URL": "postgresql://postgres:postgres@localhost:5432/myapp?schema=public"
  }
}